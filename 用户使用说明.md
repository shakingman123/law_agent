# 法律文书 Agent · 使用说明

> 面向终端用户（律师 / 公司员工）的操作指引。阅读本说明前，请确认管理员已将系统部署完成并提供访问网址。

---

## 一、如何打开网页

1. 打开浏览器（推荐 **Chrome** 或 **Edge** 最新版）。
2. 在地址栏输入管理员提供的网址（例如 `https://law.your-company.com` 或 `http://服务器IP`），回车。
3. 首次使用：点击页面上的 **「注册」**，填写邮箱、密码等信息完成注册。
4. 已有账号：直接输入邮箱、密码 **「登录」**。
5. 登录后默认进入 **工作台** 页面。

> 提示：登录状态会保持一段时间；若提示「登录过期，请重新登录」，重新登录即可，不会丢失历史记录。

---

## 二、首次使用前的配置（按角色）

### 1. 普通员工

- 需向公司管理员索取 **邀请码**。
- 点击左下角头像进入 **「设置」**，选择 **「公司」** 标签。
- 在「加入新公司」处填入邀请码 → 点击「申请加入」。
- 等待管理员审批通过后，即正式加入公司，可使用公司共享的 AI 模型。

### 2. 公司管理员

- 首次需在「设置 → 公司」申请成为公司管理员：填写公司名称、上传 **营业执照** 与 **法人授权书**。
- 成为管理员后，在「设置 → 公司」可：
  - 点击 **「重新生成」** 生成邀请码，复制后发给员工；
  - 在 **「待审批加入申请」** 中批准或拒绝员工的加入申请。

> ⚠️ 管理员 **必须** 在「设置 → 模型与 API」中配置公司使用的 LLM（如 DeepSeek）的 **API Key、Base URL、模型名称**，否则员工无法使用对话与文书撰写功能。

---

## 三、核心功能

### 1. 工作台（主界面）

工作台是日常使用最多的页面，中央为对话框，右侧为案件栏，左侧为功能导航。

- **撰写法律文书**：在对话框输入需求，例如「帮我写一份起诉状」「帮我写一份答辩状」「帮我写一份上诉状」。系统会：选定模板 → 确认案件 → 补充缺失信息 → 生成文书预览 → 确认后保存。
- **法律检索**：在对话框输入法律问题（如「民间借贷利率上限是多少」），系统检索法条、判例、相关观点并引用，参考资料可点击跳转。
- **模板栏**（对话框下方）：可选择「通用模板」或「自有模板」。自有模板支持上传 Word/PDF 文件，收藏的模板会排在前面。
- **案件栏**（右侧）：可随时切换当前正在处理的案件；点击「新增案件」可录入原告、被告、上诉法院、案件基本情况等信息。
- **左侧栏**：可快速跳转至「日程列表」「文档库」「设置」。

### 2. 日程列表

- 顶部可切换 **日 / 周 / 月** 视图，日历上以颜色标注不同事项：
  - 🔴 红色：紧急事项（举证期、上诉期等）
  - 🟠 琥珀色：一般期限
  - 🔵 蓝色：会议
- 点击 **「新建日程」** 填写：
  - 日程标题（如「XX 案 上诉期截止」）
  - 截止日期
  - 紧急程度（紧急 / 一般 / 会议）
  - **提醒时间**：可选「当天提醒」「提前1天」「提前3天」「自定义天数」
- 右侧「待办提醒」列表展示所有日程及提醒设置；每条可单独删除。

### 3. 文档库

- 按案件归档保存的文档资料（文书、PDF、图片等）。
- 支持按案件关键信息搜索；非本职权级可见的案件会标注，可申请脱敏版或向经手律师申请解锁。

### 4. 设置

| 标签 | 功能 |
|---|---|
| **基本信息** | 修改姓名、邮箱、手机号 |
| **界面** | 切换字体大小、背景颜色 |
| **公司** | 邀请码加入 / 退出；管理员可生成邀请码、审批员工加入 |
| **模型与 API** | 管理员配置公司 LLM；员工查看公司 API、切换公司/个人 API、申请使用公司 API |

---

## 四、常见问题

| 现象 | 处理方式 |
|---|---|
| 提示「登录过期」 | 重新登录即可，历史记录不会丢失 |
| 对话无响应或报错 | 联系管理员确认「模型与 API」中公司 LLM Key 已正确配置 |
| 切换页面后历史对话消失 | 登录状态下切换页面不应丢失；若丢失请刷新页面或重新登录 |
| 看不到公司 API | 确认已通过公司审批加入；管理员需先配置公司 API |

---

## 五、需要配置的其他东西（汇总）

- **管理员需配置**：公司 LLM 的 API Key / Base URL / 模型（在「设置 → 模型与 API」）。
- **员工需配置**：无；凭邀请码加入公司后即可使用公司共享模型。
- **可选个人配置**：员工也可在「设置 → 模型与 API」配置个人 API，用于个人额度内的调用。

如遇无法解决的问题，请联系公司管理员或系统部署人员。
